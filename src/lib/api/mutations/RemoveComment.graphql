mutation RemoveComment($input: RemoveCommentInput!) {
  removeComment(input: $input) {
    comment {
      id
      content {
        markdown
      }
      author {
        id
        name
        username
        profilePicture
      }
      dateAdded
      stamp
      totalReactions
      myTotalReactions
    }
  }
}
