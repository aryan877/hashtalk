mutation LikeComment($input: LikeCommentInput!) {
  likeComment(input: $input) {
    comment {
      id
      content {
        markdown
      }
      author {
        name
        username
        profilePicture
      }
      dateAdded
      stamp
      totalReactions
      myTotalReactions
    }
  }
}
