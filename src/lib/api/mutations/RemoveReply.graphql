mutation RemoveReply($input: RemoveReplyInput!) {
  removeReply(input: $input) {
    reply {
      id
      content {
        markdown
      }
      author {
        name
        username
        profilePicture
      }
      dateAdded
      stamp
      totalReactions
      myTotalReactions
    }
  }
}
