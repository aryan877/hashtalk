mutation UpdateReply($input: UpdateReplyInput!) {
  updateReply(input: $input) {
    reply {
      id
      content {
        markdown
      }
      author {
        id
        name
        username
        profilePicture
      }
      dateAdded
      stamp
      totalReactions
      myTotalReactions
    }
  }
}
